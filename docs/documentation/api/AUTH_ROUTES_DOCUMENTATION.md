# Rutas de autenticación

- `GET/POST /login`  
- `POST /logout`  
- Recuperación y verificación de email (Breeze)  
- `GET /register` — según política institucional (puede restringirse)

Tras login → `route('dashboard')` → redirección por rol.
